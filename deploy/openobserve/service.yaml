apiVersion: v1
kind: Service
metadata:
  name: openobserve
  namespace: openobserve
  labels:
    app: openobserve
spec:
  type: ClusterIP
  selector:
    app: openobserve
  ports:
  - name: http
    port: 5080
    targetPort: 5080
